package com.bijesh.challengeapplication.adapters.listeners

import com.bijesh.challengeapplication.models.DogBreeds

interface RecyclerItemSelectedListener {
    fun onRecyclerViewItemSelected(dogBreed:DogBreeds)
}